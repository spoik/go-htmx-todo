package templates

templ GenericError(component templ.Component) {
	<div hx-target="this">
		if component != nil {
			@component
		}
		<p class="text-red-500">Something went wrong. Please try again.</p>
	</div>
}
